<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Simulate Matches</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">
    <div class="container py-5">
        <h1 class="mb-4 text-center">🎮 Simulate Matches</h1>
        <div class="text-center my-4">
            <a href="/" class="btn btn-secondary btn-lg">Simulate Matches</a>
            <a href="/api/histories" class="btn btn-secondary btn-lg">Player Matches History</a>
        </div>
        <form action="/api/matches/random" method="POST" class="card p-4 shadow-sm">
            <div class="mb-3">
                <label for="matchCount" class="form-label">Number of Matches</label>
                <input type="number" class="form-control" id="matchCount" name="matchCount" min="1" value="1" required>
            </div>

            <button type="submit" class="btn btn-primary w-100">Simulate Random Match</button>
        </form>
        <form action="/api/matches/player" method="POST" class="card p-4 shadow-sm mt-4">
            <div class="mb-3">
                <label for="playerId" class="form-label">Select Player</label>
                <select class="form-select" id="playerId" name="playerId" required>
                    <% players.forEach(player=> { %>
                        <option value="<%= player.id %>">
                            <%= player.name %> (ELO: <%= player.elo %>)
                        </option>
                        <% }); %>
                </select>
            </div>

            <div class="mb-3">
                <label for="matchCount" class="form-label">Number of Matches</label>
                <input type="number" class="form-control" id="matchCount" name="matchCount" min="1" value="1" required>
            </div>

            <button type="submit" class="btn btn-primary w-100">Simulate Match With Player</button>
        </form>
        <% if (message) { %>
            <div class="alert alert-info text-center my-3">
                <%= message %>
            </div>
        <% } %>
    </div>
</body>

</html>